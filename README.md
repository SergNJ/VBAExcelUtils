# VBAExcelUtils
A few operations with MS Excel files.
VBA scripts: opening all linked files to the current Excel workbook, closing all linked files, and storing each spreadsheet from the current workbook as a separate xlxs file.
